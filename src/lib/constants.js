const API_URL = process.env.API_URL || 'https://api.glitch-cat.site';
const CDN_URL = process.env.CDN_URL || 'https://cdn.glitch-cat.site';
const SITE_URL = process.env.SITE_URL || 'https://glitch-cat.site';

module.exports = {
    API_URL,
    CDN_URL,
    SITE_URL
};